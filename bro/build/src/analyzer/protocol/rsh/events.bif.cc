// This file was automatically generated by bifcl from /home/rhost/Downloads/bro-2.4.1/src/analyzer/protocol/rsh/events.bif (plugin mode).


#include "events.bif.h"

EventHandlerPtr rsh_username; 
void BifEvent::generate_rsh_username(analyzer::Analyzer* analyzer, Connection* c, StringVal* clientUsername, StringVal* serverUsername)
	{
	// Note that it is intentional that here we do not
	// check if ::rsh_username is NULL, which should happen *before*
	// BifEvent::generate_rsh_username is called to avoid unnecessary Val
	// allocation.

	val_list* vl = new val_list;

	vl->append(c->BuildConnVal());
	vl->append(clientUsername);
	vl->append(serverUsername);

	mgr.QueueEvent(::rsh_username, vl, SOURCE_LOCAL, analyzer->GetID(), timer_mgr, c);
	} // event generation
