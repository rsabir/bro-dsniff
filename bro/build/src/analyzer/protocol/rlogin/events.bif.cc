// This file was automatically generated by bifcl from /home/rhost/Downloads/bro-2.4.1/src/analyzer/protocol/rlogin/events.bif (plugin mode).


#include "events.bif.h"

EventHandlerPtr rlogin_username; 
void BifEvent::generate_rlogin_username(analyzer::Analyzer* analyzer, Connection* c, StringVal* clientUsername, StringVal* serverUsername)
	{
	// Note that it is intentional that here we do not
	// check if ::rlogin_username is NULL, which should happen *before*
	// BifEvent::generate_rlogin_username is called to avoid unnecessary Val
	// allocation.

	val_list* vl = new val_list;

	vl->append(c->BuildConnVal());
	vl->append(clientUsername);
	vl->append(serverUsername);

	mgr.QueueEvent(::rlogin_username, vl, SOURCE_LOCAL, analyzer->GetID(), timer_mgr, c);
	} // event generation
