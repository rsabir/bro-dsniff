// This file was automatically generated by bifcl from /home/rhost/Downloads/bro-2.4.1/src/analyzer/protocol/lol/events.bif (plugin mode).


#include "events.bif.h"

EventHandlerPtr lol_event; 
void BifEvent::generate_lol_event(analyzer::Analyzer* analyzer, Connection* c)
	{
	// Note that it is intentional that here we do not
	// check if ::lol_event is NULL, which should happen *before*
	// BifEvent::generate_lol_event is called to avoid unnecessary Val
	// allocation.

	val_list* vl = new val_list;

	vl->append(c->BuildConnVal());

	mgr.QueueEvent(::lol_event, vl, SOURCE_LOCAL, analyzer->GetID(), timer_mgr, c);
	} // event generation
