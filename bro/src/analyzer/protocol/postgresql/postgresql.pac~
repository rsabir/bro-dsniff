# Generated by binpac_quickstart

# Analyzer for PostgreSQL Protocol
#  - postgresql-protocol.pac: describes the postgresql protocol messages
#  - postgresql-analyzer.pac: describes the postgresql analyzer code

%include binpac.pac
%include bro.pac

%extern{
	#include "events.bif.h"
%}

analyzer POSTGRESQL withcontext {
	connection: POSTGRESQL_Conn;
	flow:       POSTGRESQL_Flow;
};

# Our connection consists of two flows, one in each direction.
connection POSTGRESQL_Conn(bro_analyzer: BroAnalyzer) {
	upflow   = POSTGRESQL_Flow(true);
	downflow = POSTGRESQL_Flow(false);
};

%include postgresql-protocol.pac

# Now we define the flow:
flow POSTGRESQL_Flow(is_orig: bool) {

	# ## TODO: Determine if you want flowunit or datagram parsing:

	# Using flowunit will cause the anlayzer to buffer incremental input.
	# This is needed for &oneline and &length. If you don't need this, you'll
	# get better performance with datagram.

	# flowunit = POSTGRESQL_PDU(is_orig) withcontext(connection, this);
	datagram = POSTGRESQL_PDU(is_orig) withcontext(connection, this);

};

%include postgresql-analyzer.pac