# Generated by binpac_quickstart

# Analyzer for RSH Protocol
#  - rsh-protocol.pac: describes the rsh protocol messages
#  - rsh-analyzer.pac: describes the rsh analyzer code

%include binpac.pac
%include bro.pac

%extern{
	#include "events.bif.h"
%}

analyzer RSH withcontext {
	connection: RSH_Conn;
	flow:       RSH_Flow;
};

# Our connection consists of two flows, one in each direction.
connection RSH_Conn(bro_analyzer: BroAnalyzer) {
	upflow   = RSH_Flow(true);
	downflow = RSH_Flow(false);
};

%include rsh-protocol.pac

# Now we define the flow:
flow RSH_Flow(is_orig: bool) {

	# ## TODO: Determine if you want flowunit or datagram parsing:

	# Using flowunit will cause the anlayzer to buffer incremental input.
	# This is needed for &oneline and &length. If you don't need this, you'll
	# get better performance with datagram.

	# flowunit = RSH_PDU(is_orig) withcontext(connection, this);
	datagram = RSH_PDU(is_orig) withcontext(connection, this);

};

%include rsh-analyzer.pac