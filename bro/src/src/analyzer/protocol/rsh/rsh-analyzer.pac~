# Generated by binpac_quickstart

refine flow RSH_Flow += {
	function proc_rsh_message(msg: RSH_PDU): bool
		%{
		BifEvent::generate_rsh_event(connection()->bro_analyzer(), connection()->bro_analyzer()->Conn());
		return true;
		%}
};

refine typeattr RSH_PDU += &let {
	proc: bool = $context.flow.proc_rsh_message(this);
};