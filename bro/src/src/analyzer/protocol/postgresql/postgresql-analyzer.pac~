# Generated by binpac_quickstart

refine flow POSTGRESQL_Flow += {
	function proc_postgresql_message(msg: POSTGRESQL_PDU): bool
		%{
		BifEvent::generate_postgresql_event(connection()->bro_analyzer(), connection()->bro_analyzer()->Conn());
		return true;
		%}
};

refine typeattr POSTGRESQL_PDU += &let {
	proc: bool = $context.flow.proc_postgresql_message(this);
};