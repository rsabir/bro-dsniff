# Generated by binpac_quickstart

refine flow RLOGIN__Flow += {
	function proc_rlogin__message(msg: RLOGIN__PDU): bool
		%{
		BifEvent::generate_rlogin__event(connection()->bro_analyzer(), connection()->bro_analyzer()->Conn());
		return true;
		%}
};

refine typeattr RLOGIN__PDU += &let {
	proc: bool = $context.flow.proc_rlogin__message(this);
};